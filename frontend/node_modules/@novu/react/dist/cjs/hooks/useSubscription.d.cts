import { TopicSubscription, NovuError } from '@novu/js';

type UseSubscriptionProps = {
    topicKey: string;
    identifier?: string;
    onSuccess?: (data: TopicSubscription | null) => void;
    onError?: (error: NovuError) => void;
};
type UseSubscriptionResult = {
    subscription?: TopicSubscription | null;
    error?: NovuError;
    isLoading: boolean;
    isFetching: boolean;
    refetch: () => Promise<void>;
};
/**
 * Get a subscription for a topic.
 */
declare const useSubscription: (props: UseSubscriptionProps) => UseSubscriptionResult;

export { type UseSubscriptionProps, type UseSubscriptionResult, useSubscription };
