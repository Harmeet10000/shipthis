declare const useBrowserTabsChannel: <T = unknown>({ channelName, onMessage, }: {
    channelName: string;
    onMessage: (args: T) => void;
}) => {
    postMessage: (data: T) => void;
};

export { useBrowserTabsChannel };
