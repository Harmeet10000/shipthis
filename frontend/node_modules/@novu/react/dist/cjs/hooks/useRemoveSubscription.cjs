"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/hooks/useRemoveSubscription.ts
var useRemoveSubscription_exports = {};
__export(useRemoveSubscription_exports, {
  useRemoveSubscription: () => useRemoveSubscription
});
module.exports = __toCommonJS(useRemoveSubscription_exports);
var import_react = require("react");
var import_NovuProvider = require("./NovuProvider.cjs");
var useRemoveSubscription = (props = {}) => {
  const propsRef = (0, import_react.useRef)(props);
  propsRef.current = props;
  const novu = (0, import_NovuProvider.useNovu)();
  const [isRemoving, setIsRemoving] = (0, import_react.useState)(false);
  const [error, setError] = (0, import_react.useState)();
  const removeCallback = (0, import_react.useCallback)(
    async (args) => {
      const { onSuccess, onError } = propsRef.current;
      setError(void 0);
      setIsRemoving(true);
      const response = await novu.subscriptions.delete(args);
      setIsRemoving(false);
      if (response.error) {
        setError(response.error);
        onError == null ? void 0 : onError(response.error);
      } else if (response.data) {
        onSuccess == null ? void 0 : onSuccess();
      }
      return response;
    },
    [novu]
  );
  return {
    remove: removeCallback,
    isRemoving,
    error
  };
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  useRemoveSubscription
});
//# sourceMappingURL=useRemoveSubscription.cjs.map