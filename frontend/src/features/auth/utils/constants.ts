/**
 * Threshold for proactive token refresh (80% of TTL)
 */
export const TOKEN_REFRESH_THRESHOLD = 0.8;

/**
 * LocalStorage key for auth state persistence
 */
export const AUTH_STORAGE_KEY = "auth-storage";

/**
 * LocalStorage key for storing redirect URL after login
 */
export const REDIRECT_URL_KEY = "auth-redirect-url";
